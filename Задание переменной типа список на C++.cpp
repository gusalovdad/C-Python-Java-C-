#include <list> // Подключаем заголовочный файл для std::list

int main() {
    // Объявляем список целых чисел с инициализацией
    std::list<int> myNumbers = {10, 20, 30, 40};

    // Объявляем пустой список строк
    std::list<std::string> names;

    // Теперь вы можете добавлять, удалять и обрабатывать элементы в этих списках
    // ...

    return 0;
}